# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: uportal-messaging-v2
metadata:
  template: uportal-messaging-v2

services:
  uportal-messaging:
    language: java
    project: .
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
